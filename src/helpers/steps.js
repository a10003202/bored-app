export const CategoryStep = 0
export const ParticipantsStep = 1
export const PriceStep = 2
export const ActivityStep = 3

const steps = {
  category: CategoryStep,
  participants: ParticipantsStep,
  price: PriceStep,
  activity: ActivityStep,
}

export default steps;